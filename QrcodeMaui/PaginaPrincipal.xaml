<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
             x:Class="TakeFoto.PaginaPrincipal"
             Title="App A.J MEI">
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Fill"
            HorizontalOptions="Fill">
            <cv:CameraView x:Name="cameraView" WidthRequest="300" HeightRequest="300" CamerasLoaded="cameraView_CamerasLoaded"/>
            <Button Text="Smile:" Clicked="Button_Clicked"></Button>
            <Image x:Name="mySmage" WidthRequest="200" HeightRequest="200"></Image>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>